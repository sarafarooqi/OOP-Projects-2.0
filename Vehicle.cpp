#include "Vehicle.h"
#include <iostream>
#include <string>
using namespace std;


Vehicle::Vehicle(string timeOfEntry, int ID){
    this -> timeOfEntry = timeOfEntry;
    this -> ID = ID;
    this -> duration = duration;
}
